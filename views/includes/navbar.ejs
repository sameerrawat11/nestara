<style>
  .nav-link{
    color: black !important; 
  }
  .nav-link:hover{
    color: #fe424d !important;
  }
  .nh{
    font-weight: 600 !important;
    font-size: medium;
  }

  .navbar-brand{
    font-size: larger;
    margin-left: 25px;
  }

  .fa-home{
    color: black;
  }
  .fa-home:hover{
    color: #fe424d;
  }

  /* ===== SEARCH ===== */
  .search-btn{
    color: #fe424d;
    border: 1px solid #fe424d;
    border-radius: 25px;
    padding: 0 1rem;
    background: transparent;
    cursor: pointer;
    margin-right: 20px !important;
  }
  .search-btn:hover{
    background-color: #fe424d;
    color: white;
  }
  .search-btn i{
    margin-right: 0.5rem;
  }
.navbar-nav .nav-link {
  margin-left: 0.75rem;
}

  .srch-ip{
    border-radius: 25px;
    padding: 0.5rem 1.2rem;
    font-size: 1rem;
    min-width: 140px;
    border: 1px solid #fe424d;
  }

  @media (min-width: 992px) {
    .srch-ip { padding: 0.5rem 3rem; min-width: 240px; }
  }

  @media (max-width: 767px) {
    .navbar .nav-link { padding: .35rem .5rem; font-size: .95rem; }
  }

  /* ===== STYLISH HAMBURGER ===== */
  .navbar-toggler {
    border: none;
    padding: 0;
    margin-right: 10px;
  }

  .navbar-toggler:focus {
    box-shadow: none;
  }

  .hamburger {
    width: 42px;
    height: 42px;
    border-radius: 12px;
    border: 1px solid #eee;
    background: #fff;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 6px;
    padding: 10px;
    cursor: pointer;
    transition: all 0.25s ease;
  }

  .hamburger span {
    height: 2px;
    width: 100%;
    background: #fe424d;
    border-radius: 4px;
    transition: all 0.25s ease;
  }

  .hamburger:hover {
    box-shadow: 0 6px 16px rgba(0,0,0,0.12);
    transform: translateY(-1px);
  }

  /* animation when open */
  .navbar-toggler[aria-expanded="true"] .hamburger span:nth-child(1) {
    transform: rotate(45deg) translateY(8px);
  }
  .navbar-toggler[aria-expanded="true"] .hamburger span:nth-child(2) {
    opacity: 0;
  }
  .navbar-toggler[aria-expanded="true"] .hamburger span:nth-child(3) {
    transform: rotate(-45deg) translateY(-8px);
  }
</style>

<nav class="navbar navbar-expand-lg bg-body-light border-bottom sticky-top">
  <div class="container-fluid">

    <a class="navbar-brand" href="/listings">
      <i class="fa-solid fa-ship" style="display:flex; margin-top:20px;">
        <p class="ln">&nbsp;Nestara</p>
      </i>
    </a>

    <!-- STYLISH TOGGLER -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <div class="hamburger">
        <span></span>
        <span></span>
        <span></span>
      </div>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">

      <!-- SEARCH -->
     &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <div class="navbar-nav ms-auto">
        <form class="d-flex" role="search" onsubmit="return false;">
          <input
            id="searchInput"
            class="form-control me-2 srch-ip"
            type="search"
            placeholder="Search Destinations"
          />
          <button id="searchBtn" class="btn search-btn" type="button" style="font-weight: 500;">
            <i class="fa-solid fa-magnifying-glass"></i>Search
          </button>
        </form>
      </div>

    

     <!-- AUTH + EXPLORE LINKS (RIGHT SIDE) -->
<div class="navbar-nav ms-auto">

  <!-- EXPLORE (NOW HERE) -->
  <a class="nav-link el"
     href="/listings"
     style="font-size:medium; font-weight:500;"><i class="fa-solid fa-window-maximize">&nbsp;</i>Explore</a>

  <!-- NESTARA YOUR HOME -->
  <a class="nav-link nh" href="/listings/new">
    <i class="fa-solid fa-home">&nbsp;</i>Nestara your home</a>

  <% if (typeof currUser === "undefined" || !currUser) { %>
    <a class="nav-link" href="/signup">
      <i class="fa-solid fa-user-plus"></i>&nbsp;<b>Signup</b></a>
    <a class="nav-link" href="/accessacc">
      <i class="fa-solid fa-user"></i>&nbsp;<b>Login</b></a>
  <% } else { %>
    <a class="nav-link" href="/logout">
      <i class="fa-solid fa-right-from-bracket"></i>&nbsp;<b>Logout</b>
      
    </a>
  <% } %>

</div>


    </div>
  </div>
</nav>

<script>
(function(){
  try {
    const searchInput = document.getElementById('searchInput');
    const searchBtn = document.getElementById('searchBtn');
    if (!searchInput) return;

    searchInput.addEventListener('keydown', function(e){
      if (e.key === 'Enter') {
        e.preventDefault();
        searchInput.dispatchEvent(new Event('input', { bubbles: true }));
      }
    });

    if (searchBtn) {
      searchBtn.addEventListener('click', function(e){
        e.preventDefault();
        searchInput.dispatchEvent(new Event('input', { bubbles: true }));
        searchInput.focus();
      });
    }
  } catch(e) {}
})();
</script>
